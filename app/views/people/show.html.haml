%h1= @person.title
%p= @person.description

= link_to "Back", root_path
= link_to "Edit", edit_person_path
= link_to "Delete", person_path, method: :delete, data: { confirm: "Are you sure you want to delete this information?"}
